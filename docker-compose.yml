version: "3.8"

services:
  lego-manager:
    build: .
    ports:
      - "8000:8000"
    env_file:
      - .env
    volumes:
      - ./lego_sets.db:/app/lego_sets.db
      - ./sets:/app/sets
    restart: unless-stopped
